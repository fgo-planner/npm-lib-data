import { GameServantWithMetadata } from '@fgo-planner/data-core';

/**
 * MongoDB document version of the `GameServantWithMetadata` type.
 */
export type GameServantWithMetadataDocument = GameServantWithMetadata<Date>;
