
export type MasterCostumes = {

    /**
     * IDs of costumes that have been unlocked by the master.
     */
    unlocked: Array<number>;

    /**
     * IDs of unlocked costumes that were unlocked for free (using ticket, etc.).
     */
    noCostUnlock: Array<number>;

};
