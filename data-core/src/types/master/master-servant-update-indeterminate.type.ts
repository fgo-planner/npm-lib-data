/**
 * A `-1` is used to represent an indeterminate value an since all numerical
 * properties in a `MasterServant` are non-negative.
 */
export type MasterServantUpdateIndeterminate = -1;

/**
 * A `-1` is used to represent an indeterminate value an since all numerical
 * properties in a `MasterServant` are non-negative.
 */
export const MasterServantUpdateIndeterminateValue: MasterServantUpdateIndeterminate = -1;
