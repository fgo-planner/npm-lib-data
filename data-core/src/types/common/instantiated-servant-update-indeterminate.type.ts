/**
 * A `-1` is used to represent an indeterminate value an since all of the
 * numerical properties in instantiated servants are non-negative.
 */
export type InstantiatedServantUpdateIndeterminate = -1;

/**
 * A `-1` is used to represent an indeterminate value an since all of the
 * numerical properties in instantiated servants are non-negative.
 */
export const InstantiatedServantUpdateIndeterminateValue: InstantiatedServantUpdateIndeterminate = -1;
